hydra:
  admin_api: "http://localhost:5445"
  public_api: "http://localhost:5444"
workload:
  read_ratio: 100             # 💡 For every write, do ~100 reads i.e. number of reads per write
  duration_sec: 60            # 💡 Run for 60 seconds, set to 0 to run indefinitely
